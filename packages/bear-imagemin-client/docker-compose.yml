version: '3.4'

services:

    imageSquash:
       restart: "no"
       image: imagine10255/bear-imagemin-server:0.0.1
       ports:
         - "3002:3000"

networks:
    default:
        external:
            name: imdockgroup
