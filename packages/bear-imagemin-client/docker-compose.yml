version: '3.4'

services:

    imageSquash:
       restart: "no"
       image: imagine10255/bear-imagemin-api:0.0.4
       ports:
         - "3001:3000"

networks:
    default:
        external:
            name: imdockgroup
